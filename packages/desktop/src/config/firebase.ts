/**
 * Desktop Firebase Configuration
 * Uses web Firebase SDK (same as web package)
 * Note: This file is for reference. The renderer (web app) will use its own Firebase config.
 */

// Firebase will be initialized in the renderer process (web app)
// This file exists for potential main process Firebase usage

// Note: Firebase is initialized in the renderer process (web app)
// The main process uses IPC to communicate with the renderer
// For Firebase operations, use the shared package services from the renderer

