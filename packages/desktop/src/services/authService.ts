/**
 * Desktop Auth Service
 * Note: Auth is handled in the renderer process (web app)
 * This service is for main process operations if needed
 * For auth operations, use the shared authService from the renderer
 */

// Auth operations are handled by the web app renderer
// This file is kept for potential main process auth needs
export const authService = {
  // Can be extended for main process auth operations if needed
};

